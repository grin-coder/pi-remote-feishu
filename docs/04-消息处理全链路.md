# 04 消息处理全链路

> 对应源码：`src/bridge/message-handler.ts`（主流程）、`src/bridge/runtime-host.ts`（执行）
> 前置知识：`03-核心概念.md`

这一篇是整个项目**最重要**的时序讲解：一条飞书消息从收到到回复，中间发生了什么。

## 1. 入口

飞书 WebSocket 收到消息 → `channel.onMessage` 触发 → `handleFeishuMessage`。

```ts
export async function handleFeishuMessage(options: {
  config: FeishuConfig;
  channel: FeishuChannel;
  store: FeishuStore;
  manager: SessionHostManager;
  message: FeishuMessage;
}): Promise<void> {
```

## 2. 全链路时序图

```
飞书消息
  │
  ▼ ① routeConversation ── 策略检查 + 生成 sessionKey
  │      ├─ 私聊被禁用？拒绝
  │      ├─ 群聊被禁用？拒绝
  │      ├─ 群聊没@机器人？静默拒绝（不回复）
  │      ├─ 用户不在白名单？拒绝
  │      └─ 成功 → { sessionKey, identity }
  │
  ▼ ② manager.getHost ── 查找/创建 SessionHost
  │      ├─ 内存有 → 更新 lastUsedAt，返回
  │      └─ 没有 → 读 store 映射 → createRuntimeHost（开 runtime）→ 持久化
  │
  ▼ ③ processAttachments ── 附件预处理
  │      ├─ 图片 + 模型支持 → base64 图片
  │      ├─ 小文本文件 → 内容展开进 prompt
  │      └─ 大文件/其他 → 存临时文件，只给路径说明
  │
  ▼ ④ normalizeMessage ── 拼装 Pi 输入
  │      ├─ 群聊去掉 @机器人 前缀
  │      ├─ 群聊注入 sender 信息到 prompt
  │      ├─ 识别 /help /stop 等命令
  │      └─ 产出 NormalizedPiInput
  │
  ▼ ⑤ handleCommand ── 命令分支？
  │      ├─ /help → 帮助卡片
  │      ├─ /new /reset → 新会话
  │      ├─ /stop → 中断
  │      ├─ /sessions /models /status → 管理卡片
  │      └─ 不是命令 → 走第 ⑥ 步
  │
  ▼ ⑥ host.queue.enqueue ── 串行入队执行
  │      发「Pi 正在处理」停止卡
  │      设置 host.activeRun
  │      ▼
  │      runtimeHost.prompt(input)
  │        ├─ 设置 FeishuUIContext（权限确认桥）
  │        ├─ runWithFeishuContext（AsyncLocalStorage 上下文）
  │        │    └─ renderPromptStream
  │        │         ├─ session.subscribe 订阅事件
  │        │         ├─ session.prompt(text)   ← Pi 开始干活
  │        │         │    ├─ 事件流 → channel.streamMarkdown 流式输出
  │        │         │    ├─ 模型调 send_file_to_chat → 读 FeishuContext 发文件
  │        │         │    └─ 权限确认 → ui-context 发卡片 → 等按钮
  │        │         └─ 完成 → 取消订阅
  │        └─ finally：恢复原 UIContext、清理临时附件
  │      ▼
  │      更新停止卡为「已完成」/「失败」
  │      删除 activeRun、更新 lastUsedAt
```

## 3. 逐段精读

### 3.1 路由：routeConversation

```ts
const route = routeConversation(options.config, options.message);
if (!route.accepted) {
  if (route.reason !== "Bot was not mentioned") {
    await options.channel.sendText(options.message.chatId, route.reason, { replyTo: options.message.messageId });
  }
  return;
}
```

细节：**没被 @ 的群消息是静默拒绝**——不给群成员刷屏。
其他拒绝原因（私聊被禁用、白名单外）会回复原因，方便用户自查。

### 3.2 获取 Host：getHost

```ts
const host = await options.manager.getHost(route.sessionKey, route.identity);
```

注意：**先拿 host，再做附件处理**。
因为附件处理需要 `host.runtimeHost.supportsImages()` 判断模型是否支持图片。

### 3.3 命令分发

```ts
if (await handleCommand({...})) {
  await cleanupTempAttachments(...);  // 命令处理完也要清临时文件
  return;
}
```

命令处理完直接 return，不进入队列。命令系统的细节见 `06-交互与命令系统.md`。

### 3.4 入队执行（核心）

```ts
await host.queue.enqueue(async () => {
  // ① 先发「正在处理」停止卡
  const stopCard = await options.channel.sendCard(
    input.chatId,
    buildStopCard({ sessionKey: input.sessionKey, chatType: input.chatType, ... }),
    { replyTo: input.messageId },
  );

  // ② 登记 activeRun（这样 /stop 或停止按钮能找到这个任务）
  host.activeRun = {
    sessionKey: input.sessionKey,
    chatId: input.chatId,
    messageId: input.messageId,
    startedAt: new Date(),
    abort: async () => { await host.runtimeHost.abort(); },
  };

  try {
    // ③ 真正跑 Pi
    await host.runtimeHost.prompt(input);
    // ④ 成功 → 更新停止卡
    await options.channel.updateCard(stopCard.messageId, buildStopDoneCard("Pi 已完成", "回复已生成完成。"));
  } catch (error) {
    // ⑤ 失败 → 更新停止卡为失败信息，并向上抛（日志里能看到）
    await options.channel.updateCard(stopCard.messageId, buildStopDoneCard("Pi 失败", message));
    throw error;
  } finally {
    // ⑥ 无论成败：清掉 activeRun、刷新最后使用时间
    delete host.activeRun;
    host.lastUsedAt = new Date();
  }
});
```

**这段代码体现了「任务状态卡片」的设计**：
- 任务开始 → 黄卡「Pi 正在处理」+ 停止按钮
- 任务结束 → 更新同一张卡为完成/失败（灰卡）

为什么用「更新同一张卡」而不是「再发一条新消息」？
因为飞书里卡片可以原地更新，避免聊天区被刷屏。

### 3.5 runtimeHost.prompt 内部

```ts
async prompt(input: NormalizedPiInput): Promise<void> {
  const uiContext = createFeishuUIContext();          // 权限确认桥（空壳，懒注册 dialog）
  const previousUIContext = this.runtime.session.extensionRunner.getUIContext();
  this.runtime.session.extensionRunner.setUIContext(uiContext, "rpc");

  try {
    await runWithFeishuContext(
      { sessionKey, chatId, messageId, channel, uiContext, config },
      async () => {
        await renderPromptStream({
          config, channel, chatId, replyTo: input.messageId,
          session: this.runtime.session,
          runPrompt: async () => {
            await this.runtime.session.prompt(input.text, {
              streamingBehavior: "steer",
              ...(input.images.length > 0 ? { images: input.images } : {}),
            });
          },
        });
      },
    );
  } finally {
    this.runtime.session.extensionRunner.setUIContext(previousUIContext, "tui");
    await cleanupTempAttachments(this.config.files.tempDir ?? ".", this.sessionKey).catch(() => {});
  }
}
```

关键点：

1. **UIContext 切换**：执行期间把 Pi 的 UI 上下文替换为飞书版，执行完**恢复**原值。
   这是「借用」Pi 的 UI 通道，用完必须还回去。
2. **AsyncLocalStorage**：`runWithFeishuContext` 包裹住整个 prompt，让工具/回调能取到当前 chat。
3. **图片**：模型支持图片才传 `images`，否则图片在附件层已被降级为文本说明。
4. **finally 清理**：prompt 结束必然清理临时附件和 UI 上下文，即使报错。

## 4. 消息在 Pi 侧的样子（归一化后的 prompt）

### 私聊

```
用户输入：帮我写个快速排序
```

### 群聊（共享模式）

```
[Feishu group message]
Sender: 张三
Session mode: shared chat session
Reply naturally to the sender in the group. Keep the answer concise unless the user asks for detail.
Message:
帮我看一下这个报错

[Attachment text: error.log]
...文件内容...
```

这就是 `message-normalizer` 的产出——**把飞书侧的元信息织进 prompt**，
让模型知道「谁在哪个群里、以什么模式」提问。

## 5. 并发与隔离验证

同一条链路，在不同会话上并行发生：

```
t=0s  用户A 私聊发「写个网站」→ queue(dm:A) 开始跑
t=0s  群G 发「@机器人 看下报错」→ queue(group:G) 开始跑
t=1s  用户A 又发「加个登录」→ 进入 queue(dm:A) 排队
       └─ dm:A 的队列深度 = 2，group:G 完全不受影响
t=2s  用户B 点停止卡 → 只 abort 当前 sessionKey 的 activeRun
```

这一切由三件事保证：
1. **queue 隔离**：同 sessionKey 串行，不同 sessionKey 独立
2. **host 隔离**：每 sessionKey 有独立 runtime 和状态
3. **context 隔离**：AsyncLocalStorage 让每个 run 有独立的「我是谁」

## 6. 小结：链路中的每个环节用到了什么

| 环节 | 用到的模块 | 产出 |
|---|---|---|
| ① 路由 | conversation-router | sessionKey |
| ② 拿 host | session-host-manager + runtime-host | 可用的 SessionHost |
| ③ 附件 | attachments/processor | 图片/文本/临时文件说明 |
| ④ 归一化 | message-normalizer | NormalizedPiInput |
| ⑤ 命令 | message-handler 内建 | 各种卡片 |
| ⑥ 执行 | prompt-queue + runtime-host + stream-renderer + ui-context | 流式回复 + 状态卡 |

下一步：进入模块精读 `05-核心模块详解/`，从通道层开始逐模块看实现。
